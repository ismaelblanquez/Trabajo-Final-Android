<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="16dp">

    <ImageView
        android:id="@+id/view_image"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:src="@drawable/ic_launcher_background"
        android:scaleType="centerCrop"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:layout_marginEnd="16dp"
        android:layout_marginRight="16dp"/>

    <TextView
        android:id="@+id/view_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@+id/view_image"
        android:layout_marginStart="16dp"
        android:textSize="18sp"
        android:textColor="#000000"
        android:textStyle="bold"
        android:maxLines="2"
        android:ellipsize="end"
        android:layout_alignTop="@+id/view_image"/>

    <TextView
        android:id="@+id/view_price"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@+id/view_image"
        android:layout_below="@+id/view_name"
        android:layout_marginStart="16dp"
        android:textSize="20sp"
        android:textColor="#009688"
        android:textStyle="bold"/>

    <RatingBar
        android:id="@+id/view_rating"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/view_name"
        android:layout_toRightOf="@+id/view_price"
        android:numStars="5"
        android:stepSize="0.5"
        android:layout_marginStart="1dp"
        android:layout_marginTop="8dp"/>

    <TextView
        android:id="@+id/view_room"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@+id/view_rating"
        android:layout_below="@+id/view_name"
        android:layout_marginStart="16dp"
        android:textSize="16sp"
        android:textColor="#808080"
        android:layout_alignBottom="@+id/view_price"/>

</RelativeLayout>
